
import 'package:flutter/material.dart';

/// @author : istiklal

class PaymentInstrumentListScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Kayıtlı Ödeme Araçları Listesi"),
      ),
      body: Container(
        child: Card(
          child: Column(
            children: [

            ],
          ),
        ),
      )
    );
  }
}

